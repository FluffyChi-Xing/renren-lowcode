/**
 * @description HTTP status codes enumeration
 * @author FluffyChi-Xing
 */
export enum HttpCodeEnum {
  SUCCESS = 200,
  FAILED = 400,
  NOT_FOUND = 404,
  INTERNAL_SERVER_ERROR = 500,
  UN_AUTHORIZED = 401,
  FORBIDDEN = 403,
}
